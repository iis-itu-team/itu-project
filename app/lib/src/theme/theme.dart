import 'package:flutter/material.dart';

const COLOR_PRIMARY = Color(0xFFFF8730);
const COLOR_SECONDARY = Color(0xFFF4EBDC);
const COLOR_TEXT = Color(0xFF000000);

class ThemeColors {
  static const colorBun = Color.fromRGBO(0xFF, 0x87, 0x30, 1.0);
  static const colorKetchup = Color.fromRGBO(0xD8, 0x23, 0x00, 1.0);
  static const colorMeat = Color.fromRGBO(0x50, 0x23, 0x14, 1.0);
  static const colorOnion = Color.fromRGBO(0xF4, 0xEB, 0xDC, 1.0);
  static const colorSalad = Color.fromRGBO(0x1A, 0x86, 0x39, 1.0);
  static const colorCheese = Color.fromRGBO(0xFE, 0xAA, 0x00, 1.0);
  static const colorBunLight = Color.fromRGBO(0xFF, 0xA1, 0x5C, 1.0);
}

var lmao = ThemeColors.colorBun;
